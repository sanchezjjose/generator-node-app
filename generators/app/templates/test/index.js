'use strict';
/* eslint no-unused-vars: "off" */
var sinon = require('sinon');
var assert = require('assert');

describe('module', function () {
    it('should have actual tests', function () {
        assert(true); // TODO - actual tests
    });
});

